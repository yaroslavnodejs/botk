<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1" />
  <title>Просмотр пасты</title>
  <style>
    :root {
      --bg-dark: #121212;
      --bg-content: #212134;
      --text-primary: #e0e0ff;
      --text-muted: #9292b8;
      --accent: #7c3aed;
      --radius: 12px;
      --shadow: rgba(124, 58, 237, 0.45);
      --font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }
    *, *::before, *::after {
      box-sizing: border-box;
    }
    body {
      margin: 0; 
      background-color: var(--bg-dark);
      color: var(--text-primary);
      font-family: var(--font-family);
      min-height: 100vh;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      padding: 2rem 1rem;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      line-height: 1.6;
      user-select: text;
    }
    main {
      background: var(--bg-content);
      border-radius: var(--radius);
      max-width: 820px;
      width: 100%;
      padding: 2rem 3rem 3rem;
      box-shadow: 0 0 20px var(--shadow);
      display: flex;
      flex-direction: column;
    }
    h2 {
      text-align: center;
      font-weight: 900;
      font-size: 2.4rem;
      margin: 0 0 2rem 0;
      color: var(--accent);
      text-shadow: 0 0 12px #b18effcc;
      letter-spacing: 0.06em;
    }
    p.time-left {
      color: var(--accent);
      font-weight: 700;
      text-align: center;
      margin-bottom: 2rem;
      font-size: 1.1rem;
      user-select: text;
    }
    pre {
      background: #2f2d4a;
      border-radius: var(--radius);
      padding: 24px 28px;
      font-family: 'Fira Mono', Consolas, 'Courier New', monospace;
      font-size: 1.05rem;
      color: #c8c8ff;
      white-space: pre-wrap;
      word-break: break-word;
      box-shadow: inset 0 0 12px #3c3b69;
      margin-bottom: 2rem;
      line-height: 1.5;
      user-select: text;
    }
    img, video {
      max-width: 100%;
      border-radius: var(--radius);
      box-shadow: 0 8px 14px var(--shadow);
      margin-bottom: 2rem;
      user-select: none;
      display: block;
      outline-offset: 4px;
    }
    video {
      outline: none;
    }
    p.file-link {
      font-size: 1rem;
      margin-bottom: 2rem;
    }
    p.file-link a {
      color: var(--accent);
      font-weight: 700;
      text-decoration: none;
      word-break: break-word;
      transition: color 0.25s ease;
    }
    p.file-link a:hover,
    p.file-link a:focus {
      color: #d3b7ff;
      text-decoration: underline;
      outline: none;
    }
    a.back-link {
      align-self: center;
      margin-top: auto;
      padding: 14px 28px;
      border-radius: var(--radius);
      background: var(--accent);
      color: white;
      font-weight: 700;
      font-size: 1.1rem;
      text-decoration: none;
      box-shadow: 0 7px 20px #a385ffcc;
      user-select: none;
      transition: background-color 0.3s ease, box-shadow 0.3s ease;
    }
    a.back-link:hover,
    a.back-link:focus {
      background-color: #9c63ff;
      box-shadow: 0 10px 28px #c0a8ffdd;
      outline: none;
    }
    @media (max-width: 720px) {
      main {
        padding: 1.5rem 2rem 2.5rem;
        max-width: 100%;
      }
      h2 {
        font-size: 1.9rem;
      }
      pre {
        padding: 18px 22px;
        font-size: 1rem;
        margin-bottom: 1.5rem;
      }
      p.time-left {
        font-size: 1rem;
        margin-bottom: 1.5rem;
      }
      p.file-link {
        font-size: 0.95rem;
        margin-bottom: 1.5rem;
      }
      a.back-link {
        font-size: 1rem;
        padding: 12px 24px;
      }
    }
  </style>
</head>
<body>
  <main role="main" aria-labelledby="paste-title">
    <h2 id="paste-title">Содержимое пасты</h2>
    <p class="time-left" aria-live="polite"><%= timeLeft %></p>
    <% content.forEach(item => { %>
      <% if (item.type === 'text') { %>
        <pre><%= item.data %></pre>
      <% } else if (item.type === 'image') { %>
        <img src="<%= item.url || '#' %>" alt="<%= item.file_name || 'Изображение' %>" loading="lazy" />
      <% } else if (item.type === 'video') { %>
        <video controls preload="metadata" playsinline>
          <source src="<%= item.url || '#' %>" type="video/mp4" />
          Ваш браузер не поддерживает видео.
        </video>
      <% } else { %>
        <p class="file-link">Файл: <a href="<%= item.url || '#' %>" tabindex="0"><%= item.file_name || 'без имени' %></a></p>
      <% } %>
    <% }) %>
    <a href="/" class="back-link" aria-label="Создать новую пасту">← Создать новую пасту</a>
  </main>
</body>
</html>
