<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />
  <title>Создать новую пасту</title>
  <style>
    /* Современный тёмный стиль и адаптивность */
    :root {
      --bg: #121214;
      --surface: #1e1e2f;
      --accent-start: #5c33ff;
      --accent-end: #7c3aed;
      --text-primary: #e0e0ff;
      --text-secondary: #a1a1b8;
      --border-focus: #9f7aff;
      --radius: 12px;
      --shadow: rgba(0, 0, 0, 0.8);
      --font: "Segoe UI", Tahoma, Geneva, Verdana, sans-serif;
    }
    *, *::before, *::after { box-sizing: border-box; }
    body {
      margin: 0; padding: 3rem 1rem;
      font-family: var(--font);
      background: var(--bg);
      color: var(--text-primary);
      display: flex; justify-content: center; min-height: 100vh;
      line-height: 1.6; user-select: none;
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
    }
    main {
      background: var(--surface);
      max-width: 650px;
      width: 100%;
      padding: 2.5rem 3rem 3rem;
      border-radius: var(--radius);
      box-shadow: 0 16px 36px var(--shadow);
      display: flex;
      flex-direction: column;
    }
    h1 {
      font-size: 2.5rem; font-weight: 900; text-align: center;
      margin: 0 0 2.5rem 0;
      background: linear-gradient(to right, var(--accent-start), var(--accent-end));
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
      user-select: text;
      letter-spacing: 0.05em;
      text-shadow: 0 0 15px #6f54f5aa;
    }
    form {
      display: grid;
      grid-template-columns: 1fr;
      gap: 1.75rem;
      user-select: text;
    }
    label {
      font-weight: 700;
      color: var(--text-secondary);
      text-transform: uppercase;
      font-size: 0.875rem;
      margin-bottom: 0.6rem;
      user-select: text;
    }
    textarea, select, input[type="file"] {
      width: 100%;
      background: #222236;
      border: 1.5px solid #36364f;
      border-radius: var(--radius);
      padding: 1rem 1.2rem;
      font-size: 1.1rem;
      color: var(--text-primary);
      resize: vertical;
      outline-offset: 2px;
      transition: border-color 0.3s ease, box-shadow 0.3s ease, background-color 0.3s ease;
    }
    textarea {
      min-height: 160px;
      font-weight: 500;
      line-height: 1.45;
      font-family: 'Courier New', Courier, monospace;
    }
    textarea::placeholder,
    input::placeholder,
    select::placeholder {
      color: var(--text-secondary);
      font-style: italic;
    }
    textarea:focus, select:focus, input[type="file"]:focus {
      background-color: #2b2b48;
      border-color: var(--border-focus);
      box-shadow: 0 0 10px var(--border-focus);
      color: var(--text-primary);
    }
    button {
      margin-top: 1rem;
      width: 100%;
      padding: 1.05rem 0;
      font-weight: 900;
      font-size: 1.3rem;
      background: linear-gradient(45deg, var(--accent-start), var(--accent-end));
      border-radius: var(--radius);
      border: none;
      cursor: pointer;
      box-shadow: 0 6px 17px #7a4bf5ad;
      user-select: none;
      transition: background-color 0.3s ease, box-shadow 0.3s ease, transform 0.2s ease;
      color: white;
      text-shadow: 0 0 5px #a785ffaa;
    }
    button:hover, button:focus {
      background: linear-gradient(45deg, #7a4bf5, #a27fff);
      box-shadow: 0 8px 24px #a27fffcc;
      transform: scale(1.05);
      outline: none;
    }
    button:active {
      transform: scale(0.97);
      box-shadow: 0 4px 15px #7c3aedcc;
    }
    .message {
      margin-top: 2.5rem;
      padding: 1.5rem 2rem;
      background: #2f2a56;
      border-radius: var(--radius);
      color: var(--accent-end);
      font-weight: 700;
      text-align: center;
      word-break: break-word;
      box-shadow: 0 0 20px #7c3aedcc;
      user-select: text;
    }
    .paste-link a {
      color: var(--accent-end);
      font-weight: 700;
      text-decoration: none;
      cursor: pointer;
      user-select: text;
      word-break: break-word;
      transition: color 0.3s ease;
    }
    .paste-link a:hover,
    .paste-link a:focus {
      color: #d6b3ff;
      text-decoration: underline;
      outline: none;
    }
    @media (min-width: 600px) {
      form {
        grid-template-columns: 1fr 1fr;
        gap: 1.8rem 2.5rem;
      }
      label[for="paste-text"] {
        grid-column: 1 / -1;
      }
      textarea {
        grid-column: 1 / -1;
      }
      button {
        grid-column: 1 / -1;
        max-width: 300px;
        justify-self: center;
      }
    }
  </style>
</head>
<body>
  <main role="main" aria-labelledby="title">
    <h1 id="title">Создать новую пасту</h1>
    <form action="/create" method="post" enctype="multipart/form-data" novalidate>
      <div>
        <label for="paste-text">Введите текст пасты:</label>
        <textarea id="paste-text" name="text" placeholder="Введите ваш текст здесь..." rows="7" aria-describedby="tip-text" aria-label="Текст пасты"><%= typeof text !== 'undefined' ? text : '' %></textarea>
        <div id="tip-text" style="color: var(--clr-text-secondary); font-size: 0.85rem; margin-top: 0.25rem;">
          Можно оставить пустым, если будет загружен файл.
        </div>
      </div>
      <div>
        <label for="paste-type">Тип контента:</label>
        <select id="paste-type" name="type" aria-label="Выберите тип пасты">
          <option value="text" selected>Текст</option>
          <option value="image">Изображение</option>
          <option value="video">Видео</option>
          <option value="file">Файл</option>
        </select>
      </div>
      <div>
        <label for="expire">Время жизни пасты:</label>
        <select id="expire" name="expire" aria-label="Выберите время жизни пасты">
          <option value="0.5">12 часов</option>
          <option value="1" selected>24 часа</option>
          <option value="3">3 дня</option>
          <option value="7">7 дней</option>
          <option value="30">30 дней</option>
          <option value="60">60 дней</option>
        </select>
      </div>
      <div>
        <label for="file-upload">Загрузить файл (для выбранного типа):</label>
        <input id="file-upload" type="file" name="file" accept="image/*,video/*,application/pdf,.doc,.docx,.txt,.zip" aria-describedby="file-help" />
        <small id="file-help" style="color: var(--clr-text-secondary); font-size: 0.85rem;">Максимальный размер файла: 10 МБ</small>
      </div>
      <button type="submit" aria-label="Создать новую пасту">Создать пасту</button>
    </form>
    <% if (message) { %>
      <section class="message" role="alert" tabindex="0" aria-live="polite">
        <%= message %>
        <% if (paste_id) { %>
          <p class="paste-link">Просмотреть пасту: <a href="/paste/<%= paste_id %>" tabindex="0"><%= paste_id %></a></p>
        <% } %>
      </section>
    <% } %>
  </main>
  <script>
    (() => {
      const pasteType = document.getElementById('paste-type');
      const fileUpload = document.getElementById('file-upload');
      function updateFileInput() {
        if (pasteType.value === 'text') {
          fileUpload.disabled = true;
          fileUpload.value = '';
          fileUpload.setAttribute('aria-disabled', 'true');
          fileUpload.parentElement.style.opacity = '0.5';
        } else {
          fileUpload.disabled = false;
          fileUpload.removeAttribute('aria-disabled');
          fileUpload.parentElement.style.opacity = '1';
        }
      }
      pasteType.addEventListener('change', updateFileInput);
      window.addEventListener('DOMContentLoaded', updateFileInput);
    })();
  </script>
</body>
</html>
